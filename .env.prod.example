# Production Environment Variables for Registry-based Deployment
# Copy this file to .env.prod and update with your actual values

# Certificate Configuration
CERT_PASSWORD=your-certificate-password-here

# Identity Server Configuration  
CLIENT_ID=amlink-submissions-client
CLIENT_SECRET=your-secure-client-secret-here

# External API Configuration
SUBMISSION_API_URL=https://api.example.com/submissions
SUBMISSION_API_KEY=your-api-key-here

# Image Configuration (optional - defaults to latest)
CLIENT_IMAGE_TAG=latest
SERVER_IMAGE_TAG=latest
REGISTRY=ghcr.io

# Deployment Notes:
# 1. Generate strong passwords for CERT_PASSWORD and CLIENT_SECRET
# 2. Replace SUBMISSION_API_URL and SUBMISSION_API_KEY with actual values
# 3. Ensure certificates are available in ./certs/ directory
# 4. Use specific image tags for production (not 'latest')